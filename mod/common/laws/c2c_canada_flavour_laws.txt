INJECT:law_landed_voting = {
	can_enact = {
		custom_tooltip = {
			text = c2c_not_granted_responsible_government_tt
			OR = {
				NOT = { has_journal_entry = je_c2c_responsible_government }
				has_variable = c2c_responsible_government_var
			}
		}
	}
}

INJECT:law_wealth_voting = {
	can_enact = {
		custom_tooltip = {
			text = c2c_not_granted_responsible_government_tt
			OR = {
				NOT = { has_journal_entry = je_c2c_responsible_government }
				has_variable = c2c_responsible_government_var
			}
		}
	}
}

INJECT:law_census_voting = {
	can_enact = {
		custom_tooltip = {
			text = c2c_not_granted_responsible_government_tt
			OR = {
				NOT = { has_journal_entry = je_c2c_responsible_government }
				has_variable = c2c_responsible_government_var
			}
		}
	}
}

INJECT:law_universal_suffrage = {
	can_enact = {
		custom_tooltip = {
			text = c2c_not_granted_responsible_government_tt
			OR = {
				NOT = { has_journal_entry = je_c2c_responsible_government }
				has_variable = c2c_responsible_government_var
			}
		}
	}
}

law_c2c_responsible_colonial_administration = {
	group = lawgroup_governance_principles
	parent = law_colonial_administration

	icon = "gfx/interface/icons/law_icons/colonial_administration.dds"

	progressiveness = 25

	disallowing_laws = {
		law_anarchy
	}

	can_enact = {
		country_is_colonial_or_company = yes
		has_variable = c2c_responsible_government_var
	}

	is_visible = {
		country_is_colonial_or_company = yes
	}

	can_impose = {
		scope:target_country ?= {
			country_is_colonial_or_company = yes
			is_subject_of = root
		}
	}

	modifier = {
		country_legitimacy_headofstate_add = 10
		country_legitimacy_ideological_incoherence_mult = 0.2
		country_bureaucrats_pol_str_mult = 0.5
		country_officers_pol_str_mult = 0.5
		country_authority_add = 100
		country_legitimacy_govt_total_votes_add = 20
	}

	ai_will_do = {
		always = no
	}
}
