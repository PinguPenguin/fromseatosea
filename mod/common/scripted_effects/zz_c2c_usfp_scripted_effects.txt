REPLACE:usfp_underground_railroad_escapes_to_canada_effect = {
	random_state = {
		limit = {
			OR = {
				state_region = s:STATE_ONTARIO
				state_region = s:STATE_ONTARIO_PENINSULA
				state_region = s:STATE_QUEBEC
				state_region = s:STATE_SAINT_LAURENT
				state_region = s:STATE_NEW_BRUNSWICK
				state_region = s:STATE_NOVA_SCOTIA
			}
			is_slave_state = no
			owner = {
				has_law_or_variant = law_type:law_slavery_banned
				NOT = {
					has_law_or_variant = law_type:law_closed_borders
				}
				NOT = {
					is_country_type = decentralized
				}
			}
			culture_percent_state = {
				target = cu:afro_american
				value < 0.25
			}
		}
		save_scope_as = escapee_target_state
	}
	usfp_underground_railroad_slave_escape_effect = {
		VARIABLE = je_underground_railroad_canada_escapees
	}
}
