namespace = c2c_dependency

c2c_dependency.1 = {
	type = country_event
	placement = root
	event_image = {
		video = "unspecific_politicians_arguing"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_newspaper.dds"

	title = c2c_dependency.1.t
	desc = c2c_dependency.1.d
	flavor = c2c_dependency.1.f
	duration = 3
	cooldown = { days = never_fire_again_modifier_time }

	trigger = {
		OR = {
			csf_is_active_trigger = no
			is_usfp_active = no
			community_framework_is_active = no
		}
	}

	option = {
		name = c2c_dependency.1.a
		default_option = yes
	}

	option = {
		name = c2c_dependency.1.b
		highlighted_option = yes
		trigger = {
			custom_tooltip = {
				text = c2c_csf_is_missing
				csf_is_active_trigger = no
			}
		}
	}

	option = {
		name = c2c_dependency.1.c
		highlighted_option = yes
		trigger = {
			custom_tooltip = {
				text = c2c_usfp_is_missing
				is_usfp_active = no
			}
		}
	}

	option = {
		name = c2c_dependency.1.e
		highlighted_option = yes
		trigger = {
			custom_tooltip = {
				text = c2c_cmf_is_missing
				community_framework_is_active = no
			}
		}
	}
}
